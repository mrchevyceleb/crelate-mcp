[project]
name = "crelate-mcp"
version = "0.1.0"
description = "Model Context Protocol server for Crelate ATS/CRM API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]
